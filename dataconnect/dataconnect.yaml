specVersion: "v1beta"
serviceId: "nutrichef"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "nutrichef-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
